# Upstream repos we index. Fetch with scripts/fetch_repos.sh.
# 57 MB of someone else's source doesn't belong in this repo — only the
# parsed snapshots/*.json do.
vendor/

node_modules/
.next/
.vercel/
.env
.env*.local

__pycache__/
*.pyc
.venv/

# build artifact
tsconfig.tsbuildinfo


# local tool + editor state
.superbrain/
.claude/
